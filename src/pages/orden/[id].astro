---
import PublicLayout from '@/layouts/PublicLayout.astro';
import OrderDetail from '@/app/features/order/OrderDetail';

const { id } = Astro.params;
---

<PublicLayout
  title="Orden confirmada — NönDecants"
  description="Detalle y seguimiento de tu orden en NönDecants."
>
  <OrderDetail orderId={id!} client:visible />
</PublicLayout>
